# Base URL of the backend API. Leave unset to use the local dev proxy (vite.config.js)
# which forwards /api requests to http://localhost:8000.
# In production, set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://aletheia-83cc.onrender.com
VITE_API_URL=